name: Build

run-name: "Workflow CI/CD Step: cargo build"

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-relay-paratest:
    runs-on: warp-ubuntu-latest-x64-8x
    strategy:
      matrix:
        runtime: [zkverify, volta]
    name: Build relay chain and paratest (${{ matrix.runtime }})
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Relay chain and paratest build
        uses: ./.github/actions/cmd-in-docker
        with:
          command: "cargo build -p zkv-relay -p paratest-node --release --features fast-runtime,${{ matrix.runtime }}"
          use_cache: 'yes'
          cache_key: 'build-relay-paratest-${{ matrix.runtime }}'
          lld_install: 'yes'

      - name: Upload relay and paratest build binaries
        uses: actions/upload-artifact@v4
        with:
          name: "relay-paratest-bins-${{ matrix.runtime }}"
          path: |
            target/release/zkv-relay
            target/release/zkv-relay-execute-worker
            target/release/zkv-relay-prepare-worker
            target/release/paratest-node
          retention-days: 1
          overwrite: true
