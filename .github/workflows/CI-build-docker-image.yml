name: Build Docker Image

on:
  workflow_dispatch:
    inputs:
      network:
        description: "The network to build for (e.g., volta, zkverify)"
        required: true
        type: string
  workflow_call:

env:
  DOCKER_HUB_ORG: horizenlabs
  DOCKER_IMAGE_BUILD_NAME: ${{ vars.DOCKER_IMAGE_BUILD_NAME }}

jobs:
  build-docker:
    runs-on: warp-ubuntu-latest-x64-8x
    name: Build Docker image
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Docker build
        id: build-docker-image
        uses: ./.github/actions/build-docker-image
        with:
          ref: ${{ github.ref }}
          docker_hub_org: ${{ env.DOCKER_HUB_ORG }}
          docker_image_name: ${{ env.DOCKER_IMAGE_BUILD_NAME }}
          docker_image_suffix: '-${{ inputs.network }}'
          features: 'metadata-hash ${{ inputs.network }}'

    outputs:
      artifact_name: ${{ steps.build-docker-image.outputs.artifact_name }}
